// Copy this file to include/secrets.h (gitignored — never commit real
// credentials) and fill in the WiFi network you're authorized to scan.
#pragma once

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-network-password"
