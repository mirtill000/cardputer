# Custom partition table — ESP32-S3FN8 (8 MB flash, no PSRAM), OTA-capable.
#
# Two 1.6 MB app slots (ota_0/ota_1) instead of one 4 MB "factory" slot,
# so OtaUpdater (src/net/OtaUpdater.h) has an inactive slot to flash a
# new image into and fall back from if it's bad — swapped for LittleFS
# space, which still comes out ahead of the original no-OTA table's 3.9
# MB (see git history): 4.69 MB now, since two 1.6 MB app slots (3.2 MB)
# take less room than one 4 MB slot did.
#
# RISK — the one thing this assistant could not verify without a real
# build (see README's testing note): whether the actual compiled
# firmware image fits inside 0x190000 (1,638,400 bytes) per slot. If it
# doesn't, `pio run -t upload` fails loudly at the esptool.py "app image
# is too big" step — a safe, visible failure, not a bricked device — and
# the fix is to grow ota_0/ota_1 and shrink spiffs to match (both must
# change together to keep the table summing to exactly 0x800000).
#
# SubType "spiffs" below, not "littlefs": ESP-IDF's partition tool
# (gen_esp32part.py) validates SubType against a fixed keyword list
# that predates LittleFS support and has no "littlefs" entry — using it
# fails the build ("Value 'littlefs' is not valid"). This is cosmetic,
# not functional: the SubType/Name fields are just labels for the
# partition table itself, not a promise about which filesystem format
# is written inside. "spiffs" is what arduino-esp32's LittleFS library
# looks for by default (LittleFS.begin()'s partitionLabel parameter
# defaults to "spiffs" for historical drop-in-replacement reasons), and
# what PlatformIO's `uploadfs` target expects when locating where to
# flash the filesystem image — so this naming isn't just convention,
# it's what makes both of those work without extra parameters anywhere
# in this codebase.
#
# otadata MUST be at 0xE000 — a real bug found on the first real
# flash of this table (not something reviewable from source, only from
# a hardware log): PlatformIO/arduino-esp32 auto-flashes a small
# "boot_app0.bin" blob (initializes otadata to point at ota_0) at a
# HARDCODED offset of 0xE000 whenever it detects an OTA-style partition
# table (ota_0/ota_1/otadata present) — regardless of what this file
# says. An earlier version of this table put otadata at 0xF000 (with a
# phy_init partition at 0xE000, matching the old non-OTA table's
# layout), so that auto-flash silently overwrote all of phy_init and
# the first half of otadata with boot_app0.bin's bytes, leaving otadata
# corrupted and the bootloader crash-looping before it could print
# anything (same banner over and over, identical PC every time — see
# git history for the full symptom + diagnosis). There's no longer a
# phy_init entry at all: Arduino-ESP32's own reference OTA partition
# tables don't have one either (PHY calibration works without a
# dedicated partition, falling back gracefully), which is what makes
# 0xE000 free for otadata in the first place.
#
# ota_0/ota_1 offsets are 0x10000-aligned (a hard requirement for app
# partitions - the bootloader memory-maps them at 64KB granularity).
# otadata left blank/erased (as it is on a first flash before any OTA
# update has ever run) makes the bootloader's documented fallback boot
# ota_0 - so a plain `pio run -t upload` needs no special handling.
#
# Name,      Type, SubType,  Offset,   Size,     Flags
nvs,         data, nvs,      0x9000,   0x5000,
otadata,     data, ota,      0xe000,   0x2000,
ota_0,       app,  ota_0,    0x10000,  0x190000,
ota_1,       app,  ota_1,    0x1A0000, 0x190000,
spiffs,      data, spiffs,   0x330000, 0x4C0000,
coredump,    data, coredump, 0x7F0000, 0x10000,
